generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://postgres:[Bx4goi3)s123]@db.oppekuhfomxwpperjuyf.supabase.co:5432/postgres")
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  name      String?
  resumes   Resume[]
  createdAt DateTime @default(now())
}

model Resume {
  id        String   @id @default(uuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  template  String
  data      Json
  pdfUrl    String?
  createdAt DateTime @default(now())
}
